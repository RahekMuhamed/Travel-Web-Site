import { PackageLocationEnum } from './package-location-enum';
/*
describe('PackageLocationEnum', () => {
  it('should create an instance', () => {
    expect(new PackageLocationEnum()).toBeTruthy();
  });
});
*/